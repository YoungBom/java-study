package sec03.exam01;

public enum Week {
	// 열거 타입  애는 클래스가 아니라 eunm이다.
	// 열거 타입 (한정된 값을 갖는 타입) 선언 ex) 요일 성별 등등
	// 열거 상수(= 값을 변경할 수 없음)
	// 상수는 한번 값이 지정이 되면 바꿀 수없다. 반대 개념으론 변수가 있다.
	// 여기에 정의되지 않은 값은 사용(대입) 불가
	MONDAY,
	TUESDAY,
	WEDNESDAY,
	THURSDAY,
	FRIDAY,
	SATURDAY,
	SUNDAY
}
